import { markedDown } from '../../../helpers/marked-down';
import { module, test } from 'qunit';

module('Unit | Helper | marked down');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = markedDown(42);
  assert.ok(result);
});
